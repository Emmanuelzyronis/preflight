/**
 * TODO (Day 2): Wrap Account.execute to intercept calls bound for the STRK20
 * shielded pool before the wallet asks the user to sign them.
 */
export function wrapAccount(account: unknown): unknown {
  return account;
}
