services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: preflight
      POSTGRES_USER: preflight
      POSTGRES_PASSWORD: preflight
    ports:
      - "5432:5432"
    volumes:
      - preflight-postgres:/var/lib/postgresql/data

volumes:
  preflight-postgres:
